using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Baum : MonoBehaviour
{
    //public GameObject tree;
    /*public float passedTime = 0f;
    public Vector3 beginningScale = new Vector3(0.1f, 0.1f, 0.1f);*/

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    /*public void grow()
    {
        this.passedTime += Time.deltaTime;
        if (this.passedTime < 10.0f)
        {

            //for (int i = 0; i <= counter;  i++)
            //{

            this.transform.localScale = Vector3.Lerp(new Vector3(0.2f, 0.2f, 0.2f),
                                                        beginningScale, // oder:
                                                                        // Tree[i].transform.localScale,
                                                        1 / this.passedTime);
            //Debug.Log("counter: " + this.counter);
            //Debug.Log("scale " + this.counter + ": " + Tree[this.counter].transform.localScale);
            //counter++;

            //}


        }


    }*/
}
